/* ------------------------------ INFORMATIONS ------------------------------
Librairie:           <ctype.h>
Role de la fonction: test de caractères d'impression (espaces inclus).
Commentaires :       Vérifie tout caractère imprimable, y compris l'espace
Parametres :         1) c : un caractère à tester
Retourne :           1 si le caractère est imprimable, 0 sinon
---------------------------------------------------------------------------- */

#include "libft.h"

int	ft_isprint(int c)
{
	if ((c < 32) || (c > 126))
		return (0);
	return (1);
}
